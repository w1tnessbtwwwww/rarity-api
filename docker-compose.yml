services:
  api:
    container_name: rarity_api
    build:
      context: .
    restart: always
    command: ["poetry", "run", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--app-dir", "src/rarity_api"]
    env_file:
      - .env
    ports:
      - 8000:8000
    
    depends_on:
      - db
  db:
    image: postgres:16.4
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ${PROJECT_TITLE}-db:/var/lib/postgresql/data
    ports:
      - '${POSTGRES_PORT}:5432'

volumes:
  rarity-api-db:
    name: ${PROJECT_TITLE}-db
